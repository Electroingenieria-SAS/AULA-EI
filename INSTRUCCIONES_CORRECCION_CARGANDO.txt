CORRECCIÓN APLICADA

El build anterior podía quedarse en "Cargando Aula EI..." porque src/lib/supabase.ts exigía variables VITE_SUPABASE_URL y VITE_SUPABASE_PUBLISHABLE_KEY en runtime.

Cuando se publica por GitHub Pages usando Branch / Root, normalmente se suben archivos ya compilados y no existen variables de entorno VITE_* en el navegador. Eso hacía que el módulo de Supabase lanzara un error antes de que React montara la app.

Se corrigió dejando fallback público para:
- https://ipoidimevokogptydbvt.supabase.co
- sb_publishable_T2MmUM_SxiMAtGpp8mQ1NA_ZR6JWuwp

La service_role / secret key NO está en este paquete. Esa llave sigue únicamente en la Edge Function como secret SERVICE_ROLE_KEY.
