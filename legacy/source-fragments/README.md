# Fragmentos fuente incompletos

Estos archivos se conservaron porque ayudan a entender cambios recientes, pero **no forman una aplicación compilable**.

Faltan, entre otros:

- `package.json` y lockfile del proyecto fuente;
- `src/main.*` y el componente principal;
- `src/lib/certificateTools`;
- `src/lib/supabase`;
- `src/lib/types`;
- `src/lib/timeout`;
- el resto de páginas y componentes observados en el build.

No copies estos archivos sobre un proyecto nuevo suponiendo que reconstruyen el sistema. Úsalos solo como referencia al recuperar el repositorio fuente verdadero.

